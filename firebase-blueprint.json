{
  "entities": {
    "Student": {
      "title": "Student",
      "description": "Represents a student and their academic records.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "number": { "type": "string" },
        "email": { "type": "string" },
        "avatar": { "type": "string" },
        "class": { "type": "string" },
        "role": { "type": "string" },
        "attendance": { "type": "number" },
        "status": { "type": "string" },
        "grades": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "subject": { "type": "string" },
              "value": { "type": "number" },
              "date": { "type": "string" }
            }
          }
        },
        "ktsResults": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "examName": { "type": "string" },
              "date": { "type": "string" },
              "score": { "type": "number" }
            }
          }
        },
        "createdAt": { "type": "string" },
        "updatedAt": { "type": "string" }
      },
      "required": ["name", "number"]
    },
    "Message": {
      "title": "Message",
      "description": "A communication message between users.",
      "type": "object",
      "properties": {
        "sender": { "type": "string" },
        "senderRole": { "type": "string" },
        "content": { "type": "string" },
        "time": { "type": "string" },
        "recipientId": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["sender", "content", "time", "recipientId"]
    },
    "Class": {
      "title": "Class",
      "description": "A scheduled class session.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "time": { "type": "string" },
        "location": { "type": "string" },
        "classGroup": { "type": "string" },
        "image": { "type": "string" },
        "status": { "type": "string", "enum": ["ongoing", "next"] }
      },
      "required": ["title", "time"]
    },
    "Attendance": {
      "title": "Attendance",
      "description": "Daily attendance record for a student.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "studentName": { "type": "string" },
        "date": { "type": "string" },
        "status": { "type": "string", "enum": ["present", "absent"] },
        "classId": { "type": "string" }
      },
      "required": ["studentId", "date", "status"]
    },
    "Notification": {
      "title": "Notification",
      "description": "Alerts for new messages, exams, or performance changes.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "content": { "type": "string" },
        "type": { "type": "string", "enum": ["message", "exam", "performance", "system"] },
        "isRead": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "link": { "type": "string" }
      },
      "required": ["userId", "title", "content", "type", "createdAt"]
    },
    "Homework": {
      "title": "Homework",
      "description": "Assignments given by teachers to students/classes.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "deadline": { "type": "string", "format": "date" },
        "teacherId": { "type": "string" },
        "classId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "description", "deadline", "teacherId", "classId", "createdAt"]
    },
    "Announcement": {
      "title": "Announcement",
      "description": "Public announcements for all users.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "content": { "type": "string" },
        "date": { "type": "string" },
        "teacherId": { "type": "string" }
      },
      "required": ["title", "content", "date", "teacherId"]
    },
    "Attendance": {
      "title": "Attendance",
      "description": "Attendance records for a class session.",
      "type": "object",
      "properties": {
        "classId": { "type": "string" },
        "date": { "type": "string" },
        "records": { 
          "type": "object",
          "description": "Map of student IDs to their status (present, absent, late)"
        },
        "teacherId": { "type": "string" }
      },
      "required": ["classId", "date", "records", "teacherId"]
    }
  },
  "firestore": {
    "/attendance/{attendanceId}": {
      "schema": "Attendance",
      "description": "Daily attendance logs."
    },
    "/announcements/{announcementId}": {
      "schema": "Announcement",
      "description": "Global announcements."
    },
    "/homework/{homeworkId}": {
      "schema": "Homework",
      "description": "Homework assignments."
    },
    "/students/{studentId}": {
      "schema": "Student",
      "description": "Collection of students."
    },
    "/messages/{messageId}": {
      "schema": "Message",
      "description": "Chat and broadcast messages."
    },
    "/classes/{classId}": {
      "schema": "Class",
      "description": "Schedule of classes."
    },
    "/attendance/{attendanceId}": {
      "schema": "Attendance",
      "description": "Daily attendance records."
    },
    "/notifications/{notificationId}": {
      "schema": "Notification",
      "description": "User specific notifications."
    },
    "/yoneticiler/{adminId}": {
      "schema": "Message",
      "description": "Admin users collection."
    }
  }
}
